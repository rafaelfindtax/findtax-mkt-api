version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: findtax-api-standalone
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
      - API_VERSION=/api/v1
      - POSTGRES_HOST=ep-empty-cloud-a5twjwlc-pooler.us-east-2.aws.neon.tech
      - POSTGRES_PORT=5432
      - POSTGRES_USER=neondb_owner
      - POSTGRES_PASSWORD=TSXrIH4tRzi8
      - POSTGRES_DB=findtaxdb
      - POSTGRES_SCHEMA=public
      - POSTGRES_SSL=true
    ports:
      - "5001:3000"
    volumes:
      - ./:/app
      - /app/node_modules 